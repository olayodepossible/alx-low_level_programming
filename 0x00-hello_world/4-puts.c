#!/bin/bash
#include <stdio.h>

/**
 * main - prints the phrase "Programing is like building a multilingual puzzle"
 * Return: 0 if it run with no error, else non-zero digit
*/
int main (void)
{
	puts("\"Programming is like building a multilingual puzzle" );
	return (0);
}
