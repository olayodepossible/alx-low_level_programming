#include <stdio.h>

/**
 * main - print some phrase to standard output
 * Return: 0 if no error, else non-zero digit
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
